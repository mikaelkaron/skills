---
phase: 02-github-actions
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - test/workflows.test.ts
  - package.json
autonomous: true
requirements:
  - GHA-01
  - GHA-02
  - GHA-03
  - GHA-04
  - GHA-05
  - GHA-06
  - GHA-07
  - GHA-08
  - GHA-09
  - CI-01
  - CI-02
  - CI-03

must_haves:
  truths:
    - "test/workflows.test.ts exists and parses both workflow YAML files"
    - "Running the test file exits non-zero (RED) because .github/workflows/release.yml and ci.yml do not yet exist"
    - "Every structural requirement from GHA-01..GHA-09 and CI-01..CI-03 has a corresponding it() assertion"
    - "js-yaml is an explicit devDependency (not only transitive)"
  artifacts:
    - path: "test/workflows.test.ts"
      provides: "Structural assertions for both workflow YAML files"
      exports: []
    - path: "package.json"
      provides: "js-yaml as explicit devDependency"
      contains: '"js-yaml"'
  key_links:
    - from: "test/workflows.test.ts"
      to: ".github/workflows/release.yml"
      via: "readFileSync at module level"
      pattern: 'readFileSync.*release\.yml'
    - from: "test/workflows.test.ts"
      to: ".github/workflows/ci.yml"
      via: "readFileSync at module level"
      pattern: 'readFileSync.*ci\.yml'
---

<objective>
Wave 0 TDD gate: install js-yaml as an explicit devDependency and write test/workflows.test.ts with structural assertions for all 12 requirements (GHA-01..GHA-09, CI-01..CI-03). The test file must fail RED immediately (because the workflow YAML files do not yet exist), establishing the gate that Wave 1 and Wave 2 must satisfy.

Purpose: Ensure every structural requirement is verified by an automated test before any workflow file is authored. Tests act as the specification.
Output: test/workflows.test.ts (failing RED), package.json with js-yaml devDependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-github-actions/02-CONTEXT.md
@.planning/phases/02-github-actions/02-RESEARCH.md
@.planning/phases/02-github-actions/02-PATTERNS.md
@.planning/phases/02-github-actions/02-VALIDATION.md
</context>

<interfaces>
<!-- Key patterns from test/release-config.test.ts to follow exactly -->

From test/release-config.test.ts:
```typescript
import assert from "node:assert/strict";
import { describe, it } from "node:test";
// All it() callbacks are synchronous — no async needed for file I/O tests
```

From .planning/phases/02-github-actions/02-RESEARCH.md (Step ID Mapping):
| Input Name                    | Step ID String                                |
|-------------------------------|-----------------------------------------------|
| skip_install                  | @semantic-release/exec:install                |
| skip_set_workspace_versions   | @semantic-release/exec:set-workspace-versions |
| skip_build                    | @semantic-release/exec:build                  |
| skip_root                     | @semantic-release/npm:.                       |
| skip_cli                      | @semantic-release/npm:packages/cli            |
| skip_cherry_pick_filter       | @semantic-release/npm:packages/cherry-pick-filter |
| skip_tessl                    | @semantic-release/npm:packages/tessl          |
| skip_git                      | @semantic-release/git                         |
| skip_github                   | @semantic-release/github                      |
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Install js-yaml devDependency and write failing workflow tests (RED)</name>
  <files>package.json, test/workflows.test.ts</files>
  <read_first>
    - package.json — current devDependencies (must not remove existing entries; add js-yaml@4.1.1)
    - test/release-config.test.ts — exact import style, describe/it structure, assert usage to replicate
    - .planning/phases/02-github-actions/02-RESEARCH.md — Pattern 3 (TDD Structural Tests), Step ID Mapping table, full assertion list per requirement
    - .planning/phases/02-github-actions/02-PATTERNS.md — file loading pattern (module-level readFileSync + load())
  </read_first>
  <behavior>
    - RED: `node --experimental-strip-types --test 'test/workflows.test.ts'` exits non-zero because readFileSync throws ENOENT for both .github/workflows/release.yml and .github/workflows/ci.yml
    - All 12 requirement assertions are present as it() blocks (GHA-01 through GHA-09, CI-01 through CI-03)
    - GREEN (after Wave 1+2): same command exits 0
  </behavior>
  <action>
    Run `npm install --save-dev js-yaml@4.1.1` from repo root to add js-yaml as an explicit devDependency. Then create test/workflows.test.ts.

    File structure for test/workflows.test.ts:
    - Imports: `assert` from "node:assert/strict", `describe`/`it` from "node:test", `readFileSync` from "node:fs", `load` from "js-yaml"
    - Module-level: load both YAML files synchronously using readFileSync + load(), cast to `Record<string, unknown>`. These calls throw ENOENT immediately when files absent — this IS the RED state; no try/catch wrapper.
    - describe("release.yml", ...) block with it() for each of GHA-01..GHA-09:
      - GHA-01: assert `Object.keys(releaseYml.on).length === 1` and `"workflow_dispatch" in (releaseYml.on as any)` (per D-06)
      - GHA-02: assert all 9 boolean inputs exist in `(releaseYml.on as any).workflow_dispatch.inputs` with type "boolean" each: skip_install, skip_set_workspace_versions, skip_build, skip_root, skip_cli, skip_cherry_pick_filter, skip_tessl, skip_git, skip_github (per D-15)
      - GHA-03: assert `(releaseYml.jobs as any).release.steps[0].with["fetch-depth"] === 0` (per D-14; checkout is first step of release job)
      - GHA-04: assert `(releaseYml.jobs as any).release.steps[1].with` does not contain key "registry-url" (per D-12; setup-node is second step of release job)
      - GHA-05: assert `(releaseYml.jobs as any).release.permissions` has contents:"write", issues:"write", "pull-requests":"write", "id-token":"write" (per D-10)
      - GHA-06: assert `(releaseYml.jobs as any).release.concurrency["cancel-in-progress"] === false` (per D-11)
      - GHA-07: assert `(releaseYml.jobs as any).setup.steps[0].id === "assemble"` and the run script string contains `IFS='|'` (per D-07/D-15)
      - GHA-08: assert the semantic-release step (last step of release job) has env.GITHUB_TOKEN and env.NPM_TOKEN defined (per D-13)
      - GHA-09: assert `(releaseYml.jobs as any).release.if` includes "refs/heads/main" (per D-09; spot-check one branch from the full guard expression)
    - describe("ci.yml", ...) block with it() for each of CI-01..CI-03:
      - CI-01: assert `(ciYml.on as any).push.branches` includes "main" and `(ciYml.on as any).pull_request.branches` includes "main"; also assert `(ciYml.jobs as any).ci` exists (per D-02/D-03)
      - CI-02: assert `(ciYml.jobs as any).ci.steps` contains a step using "actions/setup-node@v4" with `with.cache === "npm"` (per D-05/CI-02)
      - CI-03: assert `(ciYml.jobs as any).ci.if` includes "[skip ci]" (per D-04/CI-03)

    Use `as any` casts for nested YAML access — same pattern as existing test/release-config.test.ts uses for dynamic property access. All it() callbacks are synchronous (no async/await).
  </action>
  <verify>
    <automated>node --experimental-strip-types --test 'test/workflows.test.ts' 2>&amp;1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains `"js-yaml": "^4.1.1"` (or exact "4.1.1")
    - test/workflows.test.ts exists
    - test/workflows.test.ts contains `import { load } from "js-yaml"`
    - test/workflows.test.ts contains `readFileSync(".github/workflows/release.yml"`
    - test/workflows.test.ts contains `readFileSync(".github/workflows/ci.yml"`
    - test/workflows.test.ts contains at least 12 `it(` calls covering all requirement IDs
    - `node --experimental-strip-types --test 'test/workflows.test.ts'` exits non-zero (RED — ENOENT for workflow files not yet created)
  </acceptance_criteria>
  <done>test/workflows.test.ts fails RED with ENOENT; package.json has js-yaml as devDependency; all 12 requirement assertions present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → filesystem | readFileSync reads workflow files from repo — no external input |
| npm install → registry | js-yaml package fetched from npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Tampering | npm install js-yaml | mitigate | js-yaml is [ASSUMED]-cleared in Package Legitimacy Audit (RESEARCH.md); confirmed present as transitive dep in node_modules |
| T-02-00-SC | Tampering | npm install js-yaml | accept | Package legitimacy audit in RESEARCH.md confirms js-yaml is a major ecosystem package (~13 yrs, ~100M/wk downloads) already present as a transitive dependency |
</threat_model>

<verification>
After Wave 0:
- `node --experimental-strip-types --test 'test/workflows.test.ts'` exits non-zero (RED — workflow files absent)
- `npm test` may still pass (workflows.test.ts failure on ENOENT propagates through test glob but existing tests remain green — this is acceptable; the Wave 0 test is expected to be red)
</verification>

<success_criteria>
- js-yaml@4.1.1 appears in package.json devDependencies
- test/workflows.test.ts exists with all 12 assertions (GHA-01..GHA-09, CI-01..CI-03)
- Running the test file alone exits non-zero (RED — ENOENT thrown at module level)
- No existing tests broken by this change
</success_criteria>

<output>
Create `.planning/phases/02-github-actions/02-00-SUMMARY.md` when done
</output>
