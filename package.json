{
  "name": "@mikaelkaron/skills",
  "version": "0.6.0",
  "bin": {
    "mks": "bin/run.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "files": [
    "bin",
    "oclif.manifest.json"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "prepublishOnly": "npm run build && oclif manifest .",
    "postpublish": "rm -f oclif.manifest.json",
    "pretest": "npm run build",
    "test": "node --experimental-strip-types --test 'packages/*/test/**/*.test.ts' 'test/**/*.test.ts'",
    "test:coverage": "c8 --reporter=text --reporter=lcov node --experimental-strip-types --test 'packages/*/test/**/*.test.ts' 'test/**/*.test.ts'",
    "test:types": "tsc -p tsconfig.test.json",
    "lint": "oxlint .",
    "format": "oxfmt ."
  },
  "dependencies": {
    "@mikaelkaron/skills-cli": "^0.6.0",
    "@oclif/core": "^4",
    "@oclif/plugin-plugins": "^5",
    "which": "^7.0.0"
  },
  "devDependencies": {
    "@npmcli/arborist": "^9.6.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^12.0.8",
    "@semantic-release/npm": "^13.1.5",
    "c8": "^11.0.0",
    "oclif": "^4",
    "oxfmt": "^0.51.0",
    "oxlint": "^1.66.0",
    "semantic-release": "^25.0.3"
  },
  "oclif": {
    "bin": "mks",
    "dirname": "mikaelkaron/skills",
    "pluginPrefix": "skills",
    "plugins": [
      "@oclif/plugin-plugins",
      "@mikaelkaron/skills-cli"
    ],
    "scope": "mikaelkaron"
  },
  "engines": {
    "node": ">=22.18"
  }
}
