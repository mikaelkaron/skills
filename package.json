{
  "name": "@mikaelkaron/skills",
  "version": "0.2.0",
  "bin": {
    "mks": "bin/run.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "files": [
    "bin"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "pretest": "tsc -b tsconfig.test.json",
    "test": "node --test 'packages/*/dist/test/**/*.test.js'",
    "test:types": "tsc -b tsconfig.test.json --noEmit",
    "lint": "oxlint .",
    "format": "oxfmt ."
  },
  "dependencies": {
    "@mikaelkaron/skills-cli": "^0.2.0"
  },
  "devDependencies": {
    "oxfmt": "^0.51.0",
    "oxlint": "^1.66.0"
  },
  "engines": {
    "node": ">=22.18"
  }
}
